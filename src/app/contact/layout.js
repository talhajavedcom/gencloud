export const metadata = {
  title: 'Contact | Gen Clouds - Modern Cloud & AI Solutions',
  description: 'Next-generation cloud computing and AI solutions for your business',
};

const RootLayout = ({ children }) => {
  return <>{children}</>;
};

export default RootLayout;
